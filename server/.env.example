PORT=4000
CLIENT_ORIGIN=http://localhost:5173
# Optional: raises GitHub rate limit from 60/hr to 5000/hr for server-side requests
# with no token supplied by the client. Not required — users can also paste
# their own PAT in the UI, which is sent per-request instead.
GITHUB_TOKEN=
